# Database
DATABASE_URL=sqlite:./data/bonscompte.db

# JWT Secret (change in production!)
JWT_SECRET=your-secret-key-here

# Server
HOST=0.0.0.0
PORT=8000

# Resource Limits
# Maximum number of projects a user can create (empty or 0 = unlimited)
# Set this on public instances to prevent resource exhaustion
# MAX_PROJECTS_PER_USER=3

# Logging
RUST_LOG=info,bonscompte_backend=debug
