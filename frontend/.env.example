# Frontend environment variables
#
# API base URL - defaults to http://localhost:8000 for development
# Override only if needed:
#
# - For production behind nginx reverse proxy:
#   VITE_API_BASE=/api
#
# - For docker local dev (set at build time in docker-compose.override.yml):
#   VITE_API_BASE=http://localhost:8000
#
# VITE_API_BASE=http://localhost:8000
